# NKD Diary 首页 V6 交接

## 本轮已实现
- 首页桌面端固定左 3 / 右 7，并改成左右两列独立堆叠，写心情下方不再因左侧模块高度而留白。
- 顶部展示本人头像和名字，点击进入设置；悬停头像会出现爱心与“你值得被爱，勇敢去爱”。
- 顶部通知收敛为“收信箱”和“新动态”两个大入口 Tab：图标常驻、未读角标、未读时低频动效、悬停或点击展开最近 10 条。
- 通知排序：未读优先，同状态下最新优先；点击具体内容后标记已读并深链。
- 收信箱点击未读信件前会确认是否打开；高级开信动画将在信件专题重构继续完善。
- 我们的故事时间统一为 Asia/Shanghai，显示年月日时分秒。
- “我们的距离”新增标题、双方城市级位置、最近 5 次足迹、超过 30 天未更新提醒。
- 位置更新会长期写入 location_history；同城也记录，只有 10 分钟内同城误触合并。
- 完整月历支持首页左右切月；日期格聚合信件/日记、文字心情、回忆及日历事件，点击日期在格内展开摘要，不再直接跳日记页。

## 必须执行的 Supabase SQL
在 Supabase SQL Editor 运行：
`supabase/migrations/202607260001_home_activity_and_location_history.sql`

该迁移创建位置历史表、位置写入 RPC，并为回忆、文字心情、日历事件创建伴侣动态通知。

## 本地验证
保留原项目 `.env.local`，然后运行：

```bat
npm install
npm test
npm run lint
npm run build
npm run dev
```

当前执行环境无法完成 npm install；已使用 TypeScript transpile 对 11 个改动文件做语法检查，全部通过，但这不等同于完整类型检查和构建。

## 当前边界
- 登录地址“城市变化自动提醒”仍需浏览器已授予定位权限后才能无打扰检测；本轮已完成 30 天未更新提醒与长期记录底座。
- 头像、昵称变更通知的数据库触发器尚未加入，留待设置页改造时和资料更新逻辑一起实现，避免把“空间名称修改”和“个人昵称修改”混为一类。
- 信件开封高级动效尚未完成，仅先完成具体通知选择、确认阅读和深链入口。
